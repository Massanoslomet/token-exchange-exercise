server:
  port: 8080

spring:
  application:
    name: frontend-service

sts:
  exchange-url: http://localhost:8082/exchange
  service-token: frontend-service-token

backend:
  resources-url: http://localhost:8083/api/resources